name: Docker Action Interior

# inputs:
#   TEST_SECRET:
#     description: "test secret"
#     # required: true
#     # default: "World"

runs:
  using: "docker"
  image: "./docker_dir/Dockerfile"
  env:
    TEST_SECRET: ${{ secrets.TEST_SECRET }}
  run: |
    echo "${TEST_SECRET}"