name: Docker Action Interior

inputs:
  TEST_SECRET:
    description: "test secret"
    # required: true
    # default: "World"

runs:
  using: "docker"
  image: "./docker_dir/Dockerfile"
  env:
    - ${{ inputs.TEST_SECRET }}
