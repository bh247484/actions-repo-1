name: Docker Action Interior

inputs:
  ACC_KEY:
    description: "access token"
    # required: true
    # default: "World"

runs:
  using: "docker"
  image: "./docker_dir/Dockerfile"
  env:
    ACC_KEY: ACC_KEY